name: Release

on:
  push:
    tags:
      - "v[0-9]+.[0-9]+.[0-9]+"

permissions: {}

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
    - uses: actions/checkout@v7
    - name: Set up JDK
      uses: actions/setup-java@v5
      with:
        java-version-file: '.java-version'
        distribution: 'corretto'
        cache: 'maven'
    - name: Build with Maven
      run: mvn -B package -Dquarkus.package.jar.type=uber-jar
    - name: Create release
      run: gh release create ${{ github.ref_name }} --generate-notes ./target/mail-baku-runner.jar
      env:
        GITHUB_TOKEN: ${{ github.token }}
